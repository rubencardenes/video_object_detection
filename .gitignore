# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# macOS
.DS_Store

# Virtual environments
.venv

# Local settings (personal folder paths, not shared)
settings.yaml

# Detection models (large binaries, not shared via git)
models/*.onnx
