[project]
name = "video-tools"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.3",
    "onnxruntime-gpu>=1.27.0",
    "opencv-contrib-python>=5.0.0.93",
    "pyside6>=6.11.1",
    "pyyaml>=6.0.3",
    "supervision>=0.29.1",
]

[project.scripts]
video-tools = "main:main"

[project.optional-dependencies]
tensorrt = ["tensorrt>=11.3.0.99,<12", "cuda-bindings>=13,<14"]
